\documentclass[10pt, letterpaper]{article}

% Required Packages
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{paracol}
\usepackage{geometry}
\usepackage{fontawesome5}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{enumitem}

% Fonts
\setmainfont{Inter}

% Colors
\definecolor{primaryColor}{HTML}{<< design.colors.primary_color | default("1E1E1E", true) >>}
\definecolor{textColor}{HTML}{000000}
\definecolor{whiteColor}{HTML}{FFFFFF}

% Page Setup
\geometry{
    margin=1.5cm
}
\pagestyle{empty}

% Section Titles (Right Column)
\titleformat{\section}{\large\bfseries\color{primaryColor}\uppercase}{}{0em}{}[\vspace{-0.5em}\rule{\linewidth}{1pt}]
\titlespacing*{\section}{0pt}{1.5ex}{1ex}

% Section Titles (Left Column)
\newcommand{\leftsection}[1]{
    \vspace{1.5ex}
    {\large\bfseries\color{whiteColor}\uppercase{#1}} \\
    \vspace{-0.5em}\rule{\linewidth}{1pt}
    \vspace{1ex}
}

% Custom macros
\newcommand{\cvheader}[2]{
    \begin{center}
        {\Huge \bfseries #1} \\ \vspace{0.5em}
        {\large #2}
    \end{center}
}

\begin{document}

\columnratio{0.3}
\begin{paracol}{2}

% LEFT COLUMN
\begin{leftcolumn}
\colorbox{primaryColor}{
    \begin{minipage}{\linewidth}
    \color{whiteColor}
    % Contact Info
    \leftsection{Contact}
    \begin{itemize}[label={}, leftmargin=0pt]
        << if cv.phone >>
        \item \faPhone \ << cv.phone >>
        << endif >>
        << if cv.email >>
        \item \faEnvelope \ \href{mailto:<< cv.email >>}{\textcolor{whiteColor}{<< cv.email >>}}
        << endif >>
        << if cv.location >>
        \item \faMapMarker \ << cv.location >>
        << endif >>
        << if cv.website >>
        \item \faGlobe \ \href{<< cv.website >>}{\textcolor{whiteColor}{<< cv.website >>}}
        << endif >>
    \end{itemize}

    % Social Profiles
    << if cv.social_networks >>
    \leftsection{Profiles}
    \begin{itemize}[label={}, leftmargin=0pt]
        << for network in cv.social_networks >>
        \item \faLink \ \href{<< network.url >>}{\textcolor{whiteColor}{<< network.network >>}}
        << endfor >>
    \end{itemize}
    << endif >>

    % Skills
    << if "Skills" in cv.sections >>
    \leftsection{Skills}
    << for entry in cv.sections["Skills"] >>
    \textbf{<< entry.label >>}: \\
    << entry.details >> \\
    << endfor >>
    << endif >>

    \vspace{2cm} % Add some space at the bottom
    \end{minipage}
}
\end{leftcolumn}

% RIGHT COLUMN
\begin{rightcolumn}
    \cvheader{<< cv.name >>}{<< cv.label >>}

    % Summary
    << if "Summary" in cv.sections >>
    \section*{Summary}
    << for entry in cv.sections["Summary"] >>
    << entry >>
    << endfor >>
    << endif >>

    % Experience
    << if "Experience" in cv.sections >>
    \section*{Experience}
    << for entry in cv.sections["Experience"] >>
    \noindent\textbf{<< entry.position >>} \hfill << entry.date_string >> \\
    \textit{<< entry.company >>} \hfill << entry.location >>
    << if entry.highlights >>
    \begin{itemize}
        << for highlight in entry.highlights >>
        \item << highlight >>
        << endfor >>
    \end{itemize}
    << endif >>
    \vspace{1em}
    << endfor >>
    << endif >>

    % Education
    << if "Education" in cv.sections >>
    \section*{Education}
    << for entry in cv.sections["Education"] >>
    \noindent\textbf{<< entry.degree >>} \hfill << entry.date_string >> \\
    \textit{<< entry.institution >>} \hfill << entry.location >>
    << if entry.highlights >>
    \begin{itemize}
        << for highlight in entry.highlights >>
        \item << highlight >>
        << endfor >>
    \end{itemize}
    << endif >>
    \vspace{1em}
    << endfor >>
    << endif >>

\end{rightcolumn}
\end{paracol}

\end{document}
